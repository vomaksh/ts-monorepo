import { ReactElement } from 'react'

export const HelloComponent = (): ReactElement => {
    return (
        <div>
            Hello from <pre style={{ display: 'inline' }}>HelloComponent</pre>
        </div>
    )
}
